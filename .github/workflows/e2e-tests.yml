name: E2E Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  e2e:
    timeout-minutes: 25
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-
      
      - name: Install dependencies
        run: npm ci --legacy-peer-deps
      
      - name: Run Vitest with coverage
        run: npm run test -- --coverage
        continue-on-error: true
      
      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps chromium
        env:
          PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: "0"
      
      - name: Ensure Playwright browsers (from cache)
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: npx playwright install-deps chromium
      
      - name: Run Playwright tests
        run: npx playwright test
        env:
          CI: true
        continue-on-error: true
      
      - name: Merge coverage reports
        run: node scripts/merge-coverage.js
      
      - name: Check coverage threshold (70% minimum)
        id: coverage-check
        run: |
          if [ -f "coverage/combined/summary.json" ]; then
            COVERAGE=$(cat coverage/combined/summary.json | jq '.totalCoverage' || echo '0')
            echo "coverage=$COVERAGE" >> $GITHUB_OUTPUT
            echo "📊 Coverage: $COVERAGE%"
            
            if [ "$COVERAGE" -lt 70 ]; then
              echo "❌ ERRO: Cobertura ($COVERAGE%) está abaixo do threshold mínimo (70%)"
              exit 1
            else
              echo "✅ Cobertura ($COVERAGE%) atende o threshold mínimo (70%)"
            fi
          else
            echo "⚠️ Arquivo de cobertura não encontrado"
            echo "coverage=0" >> $GITHUB_OUTPUT
          fi
      
      - name: Upload Vitest coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: vitest-coverage
          path: coverage/vitest/
          retention-days: 30
      
      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
      
      - name: Upload combined coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: combined-coverage
          path: coverage/combined/
          retention-days: 30
      
      - name: Upload coverage badges
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-badges
          path: coverage/combined/badges/
          retention-days: 30
      
      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: test-results
          path: test-results/
          retention-days: 7

      - name: Configure git for Pages deployment
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"

      - name: Deploy badges to GitHub Pages
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./coverage/combined
          destination_dir: coverage
          keep_files: true
          commit_message: "📊 Update coverage badges [skip ci]"
